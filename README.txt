Steps to run application on node server
1.open a terminal and specify the path of the directory like cd c:/work/cloned_directory_name
2.run the `node server.js` from terminal to run the app
3.open a browser and type the url like http://localhost:3000/index 